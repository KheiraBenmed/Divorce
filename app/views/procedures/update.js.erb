// Mettre à jour la frise
// alert('coucou3');
// Here you will write *javascript* that would be executed by the browser

  // Reset the form
$('.progress-bar').html("<%= j render 'shared/progress_bar'%>");
setTimeout(function() {
  progressBarEvents();
}, 100);
